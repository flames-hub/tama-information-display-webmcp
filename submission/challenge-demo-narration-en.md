# Challenge demo narration

Duration: **2 minutes 18.02 seconds**
Voice: English operating-system TTS
Captions: exact narration burned into the video and provided as `challenge-demo-en.srt`

1. TAMA Information Display is an ambient information display for a four K OLED touch screen, now enhanced with WebMCP.
2. The page registers seven tools through a thin adapter. The agent reads the real display state instead of guessing, while the original touch interface remains unchanged.
3. First, we type: Show the weather in Nara.
4. The WebMCP host invokes show weather. The tool reuses the existing weather controller and router, and the visible display immediately changes to Weather.
5. Next, we type: Show Nara Go.
6. Open web page validates the requested URL, maps it to the bundled same origin copy, and switches the display to Web. The original and simple views remain available.
7. Now we type: Display a meeting from one P M.
8. Show information renders the title and message as safe text, with priority and duration metadata. Agent Activity makes the action visible and undoable.
9. Finally, we type: Return to the original screen.
10. Show ambient restores the quiet clock and the human selected ambient category. The result comes directly from the display controller.
11. This adapter is feature detected. Browsers without document dot model context skip registration safely, and manual human changes always outrank agent actions and automatic defaults.
12. TAMA Information Display. One shared screen, controlled transparently by people and agents.
